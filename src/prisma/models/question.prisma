model Question {
  id            Int    @id @default(autoincrement())
  quizId        Int
  questionText  String
  optionA       String
  optionB       String
  optionC       String
  optionD       String
  correctAnswer String

  quiz    Quiz     @relation(fields: [quizId], references: [id])
  choices Choice[]
}
